from django.db import models

# This app is acting only as a router for viewsets/serializers that
# reference models defined in `requisitions.models`. Nothing to define here.
# (Keep this file so Django treats the app as having a models module.)
